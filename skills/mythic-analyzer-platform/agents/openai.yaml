interface:
  display_name: "Mythic Analyzer 平台维护"
  short_description: "维护前后端插件、报告工具、离线打包及稳定兼容契约"
  brand_color: "#3B82F6"
  default_prompt: "Use $mythic-analyzer-platform to extend a feature without coupling it to a Boss-specific report."

policy:
  allow_implicit_invocation: true
