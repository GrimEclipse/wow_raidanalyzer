# Boss 3 · Vashnik

Structured source key: `vashnik`.

This is a fixed-timeline encounter; phase timing does not advance early from health thresholds. Future evidence should connect 瘟疫泡沫 removal position with wave direction to determine misses, while keeping uncertain coordinate sampling separate from judgment.

Frontend descriptor: `frontend/report/plugins/venomous_abyss/vashnik/plugin.js`.
