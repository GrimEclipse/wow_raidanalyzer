# Debug entry points

Run these from the project root as modules:

```powershell
python -m tools.debug.crown_of_the_cosmos
python -m tools.debug.midnight_falls
```

They are developer conveniences. The supported product flow is `/online`; automation should use `analyze.py` or `analyzer_core.runner`.
